import {
  ANDROID_GOOGLE_ID as androidGoogleID,
  API_URL as apiUrl,
  FACEBOOK_ID as facebookID,
  IOS_GOOGLE_ID as iOSGoogleID
} from 'react-native-dotenv'

const settings = {
  apiUrl,
  facebookID,
  iOSGoogleID,
  androidGoogleID
}

export default settings